# Tools

Planned command-line utilities:

- `benchmark.py` — run synthetic and imported layer benchmarks;
- `compare_jobs.py` — compare approved Materialise/ALT artifacts;
- `inspect_binary.py` — read-only structural inspection.

Artifact tools are diagnostic only. They must not patch or emit production machine files from inferred binary fields.

