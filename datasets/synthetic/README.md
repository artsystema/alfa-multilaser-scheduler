# Synthetic datasets

M0 should create versioned JSON fixtures for:

- large rectangle;
- annulus;
- two lobes with a narrow bridge;
- sharp corners / restricted regions;
- partial L1/L2 field overlap;
- directional plume conflict;
- infeasible safe concurrency.

Fixtures are deterministic software tests, not qualified ALFA process settings.

