# ALFA samples

Place deliberately reviewed, non-sensitive sample artifacts here only when they are approved for repository storage.

The default `.gitignore` excludes other files in this directory. Preserve full internal artifact sets outside Git when they contain sensitive or large machine data.

Required minimal comparison set is defined in `docs/alfa-integration.md`.

