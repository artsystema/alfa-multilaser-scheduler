# Tests

M0 must include deterministic unit, integration, golden-output and regression coverage. Every scheduler needs fixtures that prove both valid concurrency and required idling when constraints make concurrency unsafe.

